#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {

 if (argc != 2) {
  printf("Error! Use: ./prog <number>\n");

  return 1;
 }

 unsigned char a = atoi(argv[1]);

 for (int i = 7; i >= 0; i--) {
  printf("%d", (a >> i & 1));
 }

 return 0;
}
